const DB_NAME='sievpm_eventos_offline';
const DB_VERSION=1;
const EVENT_STORE='eventos';
const PEND_STORE='pendentes';
let eventoAtual=null;
let gpsAtual={latitude:null,longitude:null,precisao:null};
function dbOpen(){return new Promise((resolve,reject)=>{const r=indexedDB.open(DB_NAME,DB_VERSION);r.onupgradeneeded=()=>{const db=r.result;if(!db.objectStoreNames.contains(EVENT_STORE))db.createObjectStore(EVENT_STORE,{keyPath:'key'});if(!db.objectStoreNames.contains(PEND_STORE))db.createObjectStore(PEND_STORE,{keyPath:'id',autoIncrement:true})};r.onsuccess=()=>resolve(r.result);r.onerror=()=>reject(r.error)})}
function tx(store){return dbOpen().then(db=>new Promise((resolve,reject)=>{const t=db.transaction(store,'readonly'),s=t.objectStore(store),req=s.getAll();req.onsuccess=()=>resolve(req.result);req.onerror=()=>reject(req.error)}))}
function ownerKey(){return localStorage.getItem('sievpm_offline_owner')||'sem-operador'}
function eventosHoje(){return tx(EVENT_STORE).then(data=>data.filter(e=>e.owner===ownerKey()&&e.data===localStorage.getItem('sievpm_offline_date')))}
function salvarPendente(item){return dbOpen().then(db=>new Promise((resolve,reject)=>{const t=db.transaction(PEND_STORE,'readwrite');t.objectStore(PEND_STORE).add(item);t.oncomplete=resolve;t.onerror=()=>reject(t.error)}))}
function lerPendentes(){return tx(PEND_STORE)}
function apagarPendente(id){return dbOpen().then(db=>new Promise((resolve,reject)=>{const t=db.transaction(PEND_STORE,'readwrite');t.objectStore(PEND_STORE).delete(id);t.oncomplete=resolve;t.onerror=()=>reject(t.error)}))}
function blobParaDataURL(blob){return new Promise((resolve,reject)=>{if(!blob)return resolve(null);const r=new FileReader();r.onload=()=>resolve(r.result);r.onerror=()=>reject(r.error);r.readAsDataURL(blob)})}
function dataURLParaBlob(data){if(!data)return null;const parts=data.split(',');const mime=(parts[0].match(/:(.*?);/)||[])[1]||'image/jpeg';const bin=atob(parts[1]);const arr=new Uint8Array(bin.length);for(let i=0;i<bin.length;i++)arr[i]=bin.charCodeAt(i);return new Blob([arr],{type:mime})}
function csrf(){const m=document.cookie.match(/(?:^|; )csrftoken=([^;]+)/);return m?decodeURIComponent(m[1]):''}
function gps(){const el=document.getElementById('gps');el.textContent='Obtendo localização GPS...';el.className='gps';if(!navigator.geolocation){el.textContent='GPS não disponível neste dispositivo.';el.className='gps err';return}navigator.geolocation.getCurrentPosition(p=>{gpsAtual={latitude:p.coords.latitude,longitude:p.coords.longitude,precisao:p.coords.accuracy};el.textContent=`✓ GPS capturado (${p.coords.latitude.toFixed(6)}, ${p.coords.longitude.toFixed(6)})`;el.className='gps ok'},()=>{gpsAtual={latitude:null,longitude:null,precisao:null};el.textContent='Não foi possível obter o GPS. Autorize a localização.';el.className='gps err'},{enableHighAccuracy:true,timeout:15000,maximumAge:0})}
function esc(v){return String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
function render(){eventosHoje().then(eventos=>{const lista=document.getElementById('lista');document.getElementById('info').textContent=`${eventos.length} evento(s) disponíveis neste dispositivo.`;if(!eventos.length){lista.innerHTML='<div class="empty">Não há eventos do dia armazenados para este operador/unidade.</div>';return}lista.innerHTML=eventos.map(e=>`<div class="card"><h2>OPO ${esc(e.opo)}</h2><p><strong>Endereço:</strong> ${esc(e.endereco)}</p><p><strong>Telefone:</strong> ${esc(e.telefone)}</p><p><strong>Solicitante:</strong> ${esc(e.solicitante)}</p><button class="btn" data-id="${e.id}">📷 Registrar atendimento</button></div>`).join('');lista.querySelectorAll('button[data-id]').forEach(b=>b.onclick=()=>abrir(Number(b.dataset.id),eventos))})}
function abrir(id,eventos){eventoAtual=eventos.find(e=>e.id===id);if(!eventoAtual)return;document.getElementById('modal-titulo').textContent=`OPO ${eventoAtual.opo}`;document.getElementById('modal-info').innerHTML=`<p><strong>Endereço:</strong> ${esc(eventoAtual.endereco)}</p><p><strong>Telefone:</strong> ${esc(eventoAtual.telefone)}</p><p><strong>Solicitante:</strong> ${esc(eventoAtual.solicitante)}</p>`;document.getElementById('cumprida').checked=true;document.getElementById('foto').value='';document.getElementById('just').value='';document.getElementById('campo-foto').style.display='block';document.getElementById('campo-just').style.display='none';document.getElementById('preview').style.display='none';document.getElementById('modal').classList.add('open');gps()}
document.getElementById('cumprida').addEventListener('change',e=>{document.getElementById('campo-foto').style.display=e.target.checked?'block':'none';document.getElementById('campo-just').style.display=e.target.checked?'none':'block';if(e.target.checked)gps()});
document.getElementById('fechar').onclick=()=>document.getElementById('modal').classList.remove('open');
document.getElementById('foto').addEventListener('change',e=>{const f=e.target.files?.[0],p=document.getElementById('preview');if(f){p.src=URL.createObjectURL(f);p.style.display='block'}});
document.getElementById('salvar').onclick=async()=>{if(!eventoAtual)return;const sim=document.getElementById('cumprida').checked;const foto=document.getElementById('foto').files?.[0]||null;if(sim&&(!foto||gpsAtual.latitude===null)){alert('Para registrar SIM, capture a foto e permita o GPS.');return}if(!sim&&!document.getElementById('just').value.trim()){alert('Informe a justificativa.');return}const pend={owner:ownerKey(),evento_id:eventoAtual.id,opo:eventoAtual.opo,cumprida:sim,justificativa:document.getElementById('just').value.trim(),latitude:gpsAtual.latitude,longitude:gpsAtual.longitude,precisao:gpsAtual.precisao,criado_em:new Date().toISOString(),foto:await blobParaDataURL(foto)};await salvarPendente(pend);document.getElementById('modal').classList.remove('open');await sincronizar();render();alert(navigator.onLine?'Registro enviado ao SIEVPM.':'Registro salvo no dispositivo. Será enviado automaticamente quando houver internet.')};
async function sincronizar(){if(!navigator.onLine)return;const pend=await lerPendentes();for(const p of pend){try{const fd=new FormData();fd.append('evento_id',p.evento_id);fd.append('cumprida',p.cumprida?'SIM':'NAO');fd.append('justificativa',p.justificativa||'');if(p.latitude!==null)fd.append('latitude',p.latitude);if(p.longitude!==null)fd.append('longitude',p.longitude);if(p.precisao!==null)fd.append('precisao',p.precisao);if(p.foto){const blob=dataURLParaBlob(p.foto);fd.append('imagem',blob,`offline_${p.evento_id}_${Date.now()}.jpg`)}const headers={'X-Requested-With':'XMLHttpRequest','X-Offline-Sync':'1'};const token=csrf();if(token)headers['X-CSRFToken']=token;const r=await fetch('/eventos-do-dia/',{method:'POST',body:fd,credentials:'same-origin',headers});if(r.ok)await apagarPendente(p.id);else if(r.status===401||r.status===403)break}catch(e){break}}const restantes=(await lerPendentes()).length;document.getElementById('sync').innerHTML=restantes?`<div class="pend">${restantes} registro(s) aguardando sincronização.</div>`:'<div class="status online">✓ Sem registros pendentes.</div>'}
window.addEventListener('online',sincronizar);render();sincronizar();
